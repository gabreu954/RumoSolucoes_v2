@model IEnumerable<ProjetoRumo_v2.Models.Copa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bebida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Bebida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CopaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CopaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CopaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
